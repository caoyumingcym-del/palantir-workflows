# Nextflow run artifacts
.nextflow/
.nextflow.log*
work/
results/
results_test/

# Synthetic smoke-test data (regenerate with bin/make_test_data.py)
test_data/

# Python
__pycache__/
*.pyc
.pytest_cache/

# Manifest backups written by perturbseq_report itself
*.bak-*

# `docker save` output for uploading to ICA's Docker Repository (System
# Settings) -- hundreds of MB, never belongs in git. See containers/SETUP.md.
*.tar
