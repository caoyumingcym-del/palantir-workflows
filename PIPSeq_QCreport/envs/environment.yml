# Conda environment for perturbseq_report_v1.3.5, for `nextflow run . -profile conda`
# (process.conda points here -- see ../nextflow.config).
#
# Mirrors perturbseq_report_v1.3.5/requirements.txt, with the "recommended"
# extras included by default: without leidenalg/igraph the pipeline silently
# falls back to k-means clustering, and without harmonypy/scrublet/pyyaml a
# few optional features are unavailable. All are worth having for a pipeline
# execution environment that is built once and reused.
name: pipseq-qcreport
channels:
  - conda-forge
  - bioconda
dependencies:
  - python=3.11
  - pip
  - numpy>=1.24
  - pandas>=2.0,<3
  - matplotlib>=3.7
  - anndata>=0.9
  - scanpy>=1.9
  - scipy>=1.10
  - scikit-learn>=1.3
  - leidenalg>=0.10
  - python-igraph>=0.10
  - h5py>=3.8
  - pyyaml>=6.0
  # scrublet depends on annoy, which has no prebuilt wheel on PyPI and needs a
  # C++ compiler to build from source -- absent from the plain micromamba
  # base image, so `pip install scrublet` failed with "g++: No such file or
  # directory". conda-forge ships a prebuilt python-annoy binary; installing
  # it here means pip finds the requirement already satisfied and never
  # tries to compile it.
  - python-annoy>=1.17
  - pip:
      - harmonypy>=0.0.9
      - scrublet>=0.2.3
